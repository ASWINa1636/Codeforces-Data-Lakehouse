name: 'codeforces_warehouse'

version: '1.0.0'

config-version: 2

profile: 'codeforces_warehouse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  codeforces_warehouse:
    staging:
      +materialized: view

    warehouse:
      +materialized: table

    marts:
      +materialized: table